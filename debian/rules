#!/usr/bin/make -f
override_dh_auto_test:
	py.test

%:
	dh $@ --with python2 --buildsystem=pybuild

override_dh_auto_install:
	dh_auto_install -O--buildsystem=pybuild -- --install-args "\"--install-scripts=/usr/lib/nagios/plugins/"\"
